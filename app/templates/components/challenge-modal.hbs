{{#modal-dialog close='closeChallengeModal' translucentOverlay=true}}
  <div class="modal-content">

  <button type="button" class="close" {{action "closeChallengeModal"}}>
    <span aria-hidden="true">×</span>
    <span class="sr-only">Close</span>
  </button>

  <div class="panel panel-primary">
    <div class="panel-heading">{{challenge.category}} - {{challenge.points}}</div>
    <h2 class="form-signin-heading">{{challenge.title}}</h2>
    <br/>
    {{challenge.description}}
    <br/>
    <br/>
    <form class="simple_form form-horizontal">
      <label for="inputFlag" class="sr-only">flag</label>
      flag: <input type="username" id="inputFlag" class="form-control" placeholder="flag" autofocus>
      <button class="btn btn-success btn-login-submit" type="submit">Submit</button>
      <button class="btn btn-danger btn-cancel-action" {{action 'closeChallengeModal'}}>Cancel</button>
    </form>
  </div>

  </div>
{{/modal-dialog}}

