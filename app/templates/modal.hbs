<!-- Show current modal -->
{{#if modal.isLogin}}
  {{login-modal authController=authController modal=modal sendLogin="login"}}
{{else}}
  {{#if modal.isRegister}}
    {{register-modal authController=authController modal=modal sendRegister="register"}}
  {{else}}
    {{#if modal.isChallenge}}
      {{challenge-modal modal=modal challenge=modal.challenge}}
    {{else}}
      {{#if modal.isTeam}}
        {{team-modal modal=modal team=modal.team}}
      {{else}}
        {{#if modal.isProfile}}
          {{profile-modal modal=modal}}
        {{else}}
          {{#if modal.isLogout}}
            {{logout-modal modal=modal logout="logout"}}
          {{/if}}
        {{/if}}
      {{/if}}
    {{/if}}
  {{/if}}
{{/if}}
