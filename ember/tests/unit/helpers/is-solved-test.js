import { isSolved } from '../../../helpers/is-solved';
import { module, test } from 'qunit';

module('Unit | Helper | is solved');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = isSolved(42);
  assert.ok(result);
});
