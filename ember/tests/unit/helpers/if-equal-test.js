import { ifEqual } from '../../../helpers/if-equal';
import { module, test } from 'qunit';

module('Unit | Helper | if equal');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = ifEqual(42);
  assert.ok(result);
});
