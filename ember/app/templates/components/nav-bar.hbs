<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
       <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
    </button>
      {{#link-to "home" class="ctf-title cursor-pointer"}}<a class="navbar-brand">{{ctf.name}}</a>{{/link-to}}
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-navbar-collapse">
      <ul class="nav navbar-nav navbar-left">
        {{#if session.isAuthenticated}}
          {{#if session.isAdmin}}
            <li class="dropdown">
              {{#link-to "admin.ctf" tagName="a" class="dropdown-toggle ctf-route cursor-pointer" ata-toggle="dropdown"}}
                Admin
                <span class="caret"></span>
              {{/link-to}}
              <ul class="dropdown-menu navbar-nav">
                {{#link-to "admin.challengeboard" tagName="li" class="ctf-route cursor-pointer"}}<a>Challengeboard</a>{{/link-to}}
                {{#link-to "admin.scoreboard" tagName="li" class="ctf-route cursor-pointer"}}<a>Scoreboard</a>{{/link-to}}
                {{#link-to "admin.teams" tagName="li" class="ctf-route cursor-pointer"}}<a>Teams</a>{{/link-to}}
              </ul>
            </li>
          {{/if}}
        {{/if}}

        {{#link-to "challenges" tagName="li" class="ctf-route cursor-pointer"}}<a>Challenges</a>{{/link-to}}
        {{#link-to "scoreboard" tagName="li" class="ctf-route cursor-pointer"}}<a>Scoreboard</a>{{/link-to}}
        {{#link-to "about" tagName="li" class="ctf-route cursor-pointer"}}<a>About</a>{{/link-to}}
      </ul>

      <ul class="nav navbar-nav navbar-right">
      {{#if session.isAuthenticated}}
        <li class="ctf-route cursor-pointer"><a {{action "openProfileModal"}}>Profile</a></li>
        <li class="ctf-route cursor-pointer"><a {{action "sendLogout"}}>Logout</a></li>
      {{else}}
          <li class="ctf-route cursor-pointer"><a {{action "openRegisterModal"}}>Register</a></li>
        <li class="ctf-route cursor-pointer"><a {{action "openLoginModal"}}>Login</a></li>
      {{/if}}
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
