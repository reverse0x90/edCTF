{{nav-bar ctf=ctf modal=modal session=authController.session logout='logout'}}

{{partial 'modal'}}

<div class="main">
    {{#if session.isAuthenticated}}
      {{#if session.isAdmin}}	
       <div class="sidebar">
        {{side-bar}}
       </div>
      {{/if}}
    {{/if}}
  <div class="content">
    <div class="container">
      {{outlet}}
    </div>
  </div>
</div>
