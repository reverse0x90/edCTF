{{nav-bar ctf=ctf modal=modal session=authController.session logout='logout'}}

{{partial 'modal'}}

<div class="main">
  <div class="sidebar">
    {{#if session.isAuthenticated}}
      {{#if session.isAdmin}}
        {{side-bar}}
      {{/if}}
    {{/if}}
  </div>
  <div class="content">
    <div class="container">
      {{outlet}}
    </div>
  </div>
</div>